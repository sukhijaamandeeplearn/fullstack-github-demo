name: Test CI

on:
  push:
    branches: [test]
  pull_request:
    branches: [test]

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install server dependencies
        run: |
          cd server
          npm install

      - name: Install client dependencies
        run: |
          cd client
          npm install

      - name: Simulate tests
        run: echo "✅ Running test CI for TEST environment"

